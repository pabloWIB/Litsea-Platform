import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Editar vacante — Litsea Empleos', robots: { index: false } }
export default function EditarVacantePage() { return null }
